> do not add buttons or any UI elements without backend functionality.
> add full navigation and routing in both directions for a feature: forward (pressing/tapping on a button, adding item, ) and backward (return, item deletion)
> 